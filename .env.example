# Copie para .env e preencha. O .env real fica fora do git (ver .gitignore).

# Chave da Anthropic (usada pelo roteador de modelos)
ANTHROPIC_API_KEY=

# GitHub (adaptador IssueTracker / CodeHost)
GITHUB_TOKEN=
GITHUB_REPO=MariaHilmar/avoa-ai

# Banco (Fase 0 pode rodar sem — usa memória)
DATABASE_URL=
